var Prove = {	// 邮编验证	postCode: function(num) {		var re = /^\d{6}$/;		if (re.test(num)) {			return true;		} else {			return false;		}	},	// 身份证号码为15位或者18位，15位时全为数字，18位前17位为数字，最后一位是校验位，可能为数字或字符X 	idNum : function(num){			var reg = /(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/;		if( reg.test(num) ){   			return true;  		}else{			return false;		}	},	//手机号码验证	phoneNum : function(num){		var reg = /^\d{11}$/;		if( reg.test(num) ){			return true;		}else{			return false;		}	},	//验证中文字符	chineseWord : function(word){		var reg = /^[\u4E00-\u9FA5]+$/;		if( reg.test(word) ){			return true;		}else{			return false;		}	},	//日期格式19880303	dataStyle : function(num){		var reg = /^\d{8}$/;		if( reg.test(num) ){			return true;		}else{			return false;		}	},	//密码验证	passWord : function(str){		var reg = /^[a-zA-Z0-9]{6,16}$/;		if( reg.test(str) ){			return true;		}else{			return false;		}	},	//邮箱验证	eMail : function(str){		var emailRe = /^\w+((-w+)|(\.\w+))*\@[A-Za-z0-9]+((\.|-)[A-Za-z0-9]+)*\.[A-Za-z0-9]+$/;		if( emailRe.test(str) ){			return true;		}else{			return false;		}	}}